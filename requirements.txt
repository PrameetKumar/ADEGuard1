# Core (always required)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# UI
streamlit>=1.30

# --- Full production stack (optional but recommended) ---
# When these are installed AND internet/model-hub access is available,
# every module in src/ auto-upgrades from its lightweight fallback to
# the real model with zero code changes (see try/except ImportError
# guards in ner.py, severity.py, clustering.py, explain.py).
torch>=2.1
transformers>=4.38          # BioBERT / biomedical NER + classification
sentence-transformers>=2.5  # Sentence-BERT embeddings for clustering
hdbscan>=0.8.33              # density-based clustering
shap>=0.45
lime>=0.2.0.1

# Annotation tooling (run separately, not a Python import dependency)
# label-studio>=1.12
